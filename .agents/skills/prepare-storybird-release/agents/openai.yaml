interface:
  display_name: "Storybird Release"
  short_description: "Prepare releases and publish pushed tags with local gh"
  default_prompt: "Use $prepare-storybird-release for the requested release stage. Prepare verified notes and artifacts locally; after the user reports the prepared version tag was pushed, verify that tag and continue publication with local macOS gh."
