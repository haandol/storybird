# ADR 0002: 클릭 이벤트를 인터랙티브 단계로 변환한다

Date: 2026-09-02

## Status

Accepted (2026-09-02)

## Context

Storybird의 핵심 결과물은 영상이 아니라 사용자가 클릭하며 진행하는 데모다. 따라서 녹화 중
각 클릭의 위치와 그 클릭으로 나타난 화면을 순서대로 연결해야 한다. 빠른 연속 클릭과 녹화 종료가
겹치면 단계 순서가 바뀌거나 마지막 클릭이 사라질 수 있다.

## Decision Drivers

- 최종 플레이어가 실제 클릭 위치에서 다음 화면으로 이동해야 한다.
- 선택한 소스 밖의 클릭은 기록하면 안 된다.
- 여러 클릭이 들어와도 저장 순서는 사용자 입력 순서를 따라야 한다.
- 사용자가 Stop을 누른 직전 클릭도 저장을 마칠 기회를 가져야 한다.
- 키보드 입력은 제품 범위도 권한 범위도 아니다.

## Decision

세션 시작 화면을 첫 단계로 저장한다. 선택 소스 안의 마우스 클릭이 오면 캡처 좌표계에서 위치를
정규화하고, 클릭 결과 화면을 다음 단계로 저장한 뒤 이전 단계에 그 새 단계를 가리키는 핫스팟을
추가한다. 클릭 저장은 하나의 직렬 큐에서 처리한다. Stop은 새 클릭 수집을 먼저 끊고 큐를 모두
비운 뒤 스트림을 닫는다.

### Requirement Contract

- 입력은 왼쪽/오른쪽 mouse-down만 수집하며 키 이벤트는 관찰하지 않는다.
- 클릭 좌표는 선택한 소스의 top-left 기반 캡처 프레임에 대해 0...1로 정규화한다.
- 선택 범위 밖의 클릭은 무시한다.
- 단계와 핫스팟 연결은 클릭 순서를 보존한다.
- 새 단계 자산이 저장되기 전에는 프로젝트가 그 자산을 참조하지 않는다.
- Stop은 처리 중인 클릭 저장을 기다린다.

## 대안 검토

### 전체 영상을 저장한 뒤 클릭 구간을 사후 분석

모션을 완전하게 보존하지만 영상 인코딩, 프레임 추출, 저장 용량과 처리 시간이 커진다. 요구사항은
클릭 기반 단계이므로 불필요한 정보를 대량 저장해 채택하지 않았다.

### 브라우저 DOM과 이벤트를 직렬화

웹에서는 더 정교한 상호작용을 재생할 수 있지만 네이티브 앱과 데스크톱 창을 지원하지 못하고
민감한 DOM·인증 데이터를 더 넓게 수집한다. 범용 macOS 창 녹화 목표와 맞지 않는다.

### 클릭마다 독립 비동기 저장

처리량은 높지만 완료 순서가 입력 순서와 달라지고 Stop 직전 작업이 세션 종료 뒤 실행될 수 있어
채택하지 않았다.

## Consequences

- 출력이 편집 가능한 화면/핫스팟 그래프로 바로 만들어진다.
- 전환 애니메이션 자체는 보존하지 않는다.
- 매우 빠른 연속 클릭은 사용자가 본 중간 상태보다 나중 상태를 캡처할 수 있다.

## Related

- [썸네일 갤러리에서 녹화 소스를 선택한다](./0001-thumbnail-source-selection.md)
- [에디터와 플레이어가 같은 데모 모델을 사용한다](../authoring/0001-editor-and-player.md)
